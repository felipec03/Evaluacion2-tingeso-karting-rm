server:
  port: 8091

spring:
  application:
    name: ms-tarifasconfig
  datasource:
    url: jdbc:postgresql://tingeso-entrega2.postgres.database.azure.com:5432/tarifasdb?sslmode=require
    username: postgres
    password: Uggm9966.
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-server-service:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}