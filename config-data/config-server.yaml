server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/felipec03/Evaluacion2-tingeso-karting-rm
          default-label: main
          search-paths: config-data